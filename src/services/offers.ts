import { BaseService, IClientInitOptions } from '../client.js';

export class OfferService extends BaseService {
  constructor(options: IClientInitOptions) {
    super(options);
  }

  public GetAll = async () => {};

  public GetById = async () => {};
}
